# تصميم واجهة Messaging Gateway

## التوجه العام

يستهدف التصميم استخدام الهاتف في الوضع العمودي بنسبة 9:16 وبيد واحدة، مع اعتماد بنية واضحة شبيهة بتطبيقات iOS الأصلية: عنوان واضح، مساحات مريحة، أزرار قابلة للمس، وحالات مرئية لا تعتمد على اللون وحده. يدعم التصميم الوضعين الفاتح والداكن عبر رموز الألوان المركزية في `theme.config.js`.

## الشاشات

| الشاشة | المحتوى والوظيفة |
|---|---|
| الرسائل | بوابة للوصول إلى WhatsApp وسجل الرسائل وسجلات SMS وإدارة الاتصال، مع ملخص مختصر للرسائل الأخيرة. |
| سجلات تسليم SMS | قائمة رسائل SMS الصادرة فقط، بطاقات ملخص للحالات، مرشح حالة، تحديث، وإعادة محاولة للرسائل الفاشلة. |
| WhatsApp | تشغيل ومتابعة جلسة WhatsApp Web دون عرض سجلات SMS داخلها. |
| السجل العام | عرض تاريخ الرسائل العام عند الحاجة، مع تمييز نوع القناة بوضوح. |
| الرسائل الفاشلة | عرض الرسائل الفاشلة وإتاحة إعادة المحاولة عبر القناة الأصلية فقط. |
| إدارة الاتصال | ضبط اتصال Socket.io وحالة الجهاز والخادم. |

## شاشة سجلات تسليم SMS

يحتوي الجزء العلوي على عنوان «سجلات SMS» وزر تحديث صغير في الجهة المقابلة. أسفل العنوان يظهر وصف قصير يوضح أن القائمة تعرض رسائل الشريحة المحلية فقط. تعرض أربعة مؤشرات صغيرة: الكل، تم الإرسال، قيد الانتظار، وفشل. لا تعرض المؤشرات أرقاماً وهمية؛ تعتمد على SQLite المحلية.

تأتي بعد ذلك مرشحات أفقية قابلة للتمرير: الكل، تم الإرسال، قيد الانتظار، جارٍ المعالجة، وفشل. كل بطاقة سجل تعرض الرقم بعد إخفاء جزء منه، نص الرسالة في سطرين كحد أقصى، وقت الإنشاء، وعدد المحاولات. تستخدم الحالة نصاً وأيقونة ولوناً دلالياً: الأخضر للإرسال المؤكد، الأصفر للانتظار والمعالجة، والأحمر للفشل.

يظهر زر «إعادة المحاولة» داخل السجل الفاشل فقط. يعيد الزر إدخال السجل إلى مسار SMS الحقيقي، ولا يفتح WhatsApp ولا يعتبر العملية ناجحة قبل تأكيد طبقة DirectSms. عند عدم وجود سجلات تعرض الشاشة حالة فارغة تشرح أن الرسائل الجديدة ستظهر هنا. وعند فشل القراءة تعرض رسالة خطأ مع زر إعادة المحاولة.

## تدفقات الاستخدام الرئيسية

1. يفتح المستخدم تبويب «الرسائل» ثم يختار «سجلات SMS».
2. تُحمّل الشاشة سجلات `type = sms` و`direction = outbound` من قاعدة SQLite، مرتبة من الأحدث إلى الأقدم.
3. يختار المستخدم مرشحاً مثل «فشل» لرؤية السجلات المطابقة فقط.
4. يضغط «إعادة المحاولة» لسجل فاشل؛ تتحول الحالة إلى قيد الانتظار، ثم تتم معالجة SMS عبر المسار الأصلي.
5. بعد استلام النتيجة، تُحدّث البطاقة إلى «تم الإرسال» أو «فشل» مع سبب مختصر قابل للقراءة.

## الألوان

| الاستخدام | اللون |
|---|---|
| الإجراء الأساسي | `#0A7EA4` أزرق SGH هادئ |
| خلفية التطبيق | `#FFFFFF` فاتح و`#151718` داكن |
| سطح البطاقات | `#F5F5F5` فاتح و`#1E2022` داكن |
| تم الإرسال | `#22C55E` فاتح و`#4ADE80` داكن |
| قيد الانتظار | `#F59E0B` فاتح و`#FBBF24` داكن |
| فشل | `#EF4444` فاتح و`#F87171` داكن |
| نص ثانوي وحدود | `#687076` و`#E5E7EB` في الوضع الفاتح |

## مبادئ الوصول والسلامة

تستخدم الواجهة نص الحالة والأيقونة إلى جانب اللون، وتوفر مناطق لمس لا تقل عن حجم مناسب للاستخدام بيد واحدة. تُخفى أرقام المستلمين جزئياً، ويُعرض سبب الفشل دون تضمين أسرار أو رموز مصادقة. تعتمد القوائم على `FlatList`، وتستخدم `ScreenContainer` لمعالجة Safe Area، ولا تُرسل الشاشة رسائل حقيقية أثناء الاختبارات.
