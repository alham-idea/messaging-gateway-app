# تكامل لوحة الإدارة المستقلة

## المصدر المعتمد

لوحة الإدارة تُطوّر وتُنشر من المستودع المستقل [alham-idea/messaging-gateway-admin](https://github.com/alham-idea/messaging-gateway-admin). لا يحتفظ مستودع الخادم بمصدر واجهة إدارة مكرر أو بملفات بناء ثابتة للوحة.

| العنصر | القيمة |
|---|---|
| مستودع الواجهة | `https://github.com/alham-idea/messaging-gateway-admin.git` |
| نطاق لوحة الإدارة | `https://msgatewayadm-4pkhhml8.manus.space` |
| خادم API | `https://msg-gateway-7lqw9uuq.manus.space` |
| مسار المصادقة | `POST /api/admin/login` |
| مسارات الإدارة | `/api/trpc/admin.*` |
| اتصال Socket.io | يتم عبر الخادم الحالي وفق إعدادات عميل المستودع المستقل |

## مسار التوافق القديم

للحفاظ على الروابط والإشارات القديمة، يعيد الخادم التوجيه من `/admin` ومن أي مسار فرعي تحته إلى النطاق المستقل. هذا المسار لا يخدم ملفات HTML أو JavaScript محلية، ولا يعيد استخدام نسخة قديمة من لوحة الإدارة.

## قواعد النشر

يجب نشر لوحة الإدارة من مستودعها المستقل فقط. أما نشر الخادم فيقتصر على API وSocket.io وخدمات الأعمال. عند تغيير نطاق اللوحة يجب تحديث قيمة CORS في بيئة الخادم، وتحديث عنوان إعادة التوجيه في `server/_core/index.ts`، ثم اختبار تسجيل الدخول وطلبات tRPC وSocket.io من النطاق الجديد.

لا تتصل الواجهة مباشرة بقاعدة البيانات؛ تمر جميع عمليات الإدارة عبر API الخادم مع مصادقة JWT وصلاحيات المدير. لا تُحذف ملفات قاعدة البيانات أو تُجرى هجرات بسبب تغيير استضافة الواجهة.

## تحقق ما بعد النشر

بعد نشر الخادم والواجهة، يجب التحقق من استجابة `/api/health`، وترويسة `Access-Control-Allow-Origin` للنطاق الصحيح، وتسجيل الدخول، ثم قراءة Dashboard وUsers وSubscriptions وInvoices وSettings. يجب أن يعيد `/admin` تحويلة 302 إلى النطاق المستقل بدلاً من تقديم ملفات محلية.
